name: wifi_iot
description: Flutter plugin which can handle WiFi connections (AP, STA)
version: 0.0.1
author: Florian
homepage: https://github.com/alternadom/WiFiFlutter

environment:
  sdk: '>=2.0.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter

flutter:
  plugin:
    androidPackage: com.alternadom.wifiiot
    pluginClass: WifiIotPlugin
    androidX: true
